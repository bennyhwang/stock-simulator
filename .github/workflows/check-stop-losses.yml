name: Check Stop-Loss Daily

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Call stop-loss checker
        run: |
          curl -s -X POST "https://fuuwjceawowojecaqfru.supabase.co/functions/v1/check_stop_losses" \
            -H "Authorization: Bearer sb_publishable_M3lmOrr1QDDkkKE3r9q7qQ_mVnLlYtQ" \
            -H "Content-Type: application/json" \
            -d '{}'
